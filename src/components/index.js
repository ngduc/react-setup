import PxPopover from './PxPopover/PxPopover';

/**
 * This is an index file for all components.
 * Usage: we can have one line to import multiple components, e.g.: import { SideMenu, Header } from './components/index'
 * @type {Object}
 */
module.exports = {
  PxPopover
};
